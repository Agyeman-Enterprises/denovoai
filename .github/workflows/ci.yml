name: ci-stub

on:
  push:
  pull_request:

jobs:
  lint-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
      - name: Demo (stub)
        run: node services/orchestrator/demo.js --dry-run || true
