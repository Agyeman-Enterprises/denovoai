Given the ProductSpec, produce SystemDesign JSON with:
- services (name, responsibilities)
- data_models (name, fields)
- api_endpoints (method, path, description)
- auth_model (strategy, roles)
- observability (metrics, logs)
Do not generate code.
