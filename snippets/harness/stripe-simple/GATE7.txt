DENOVO STRIPE SIMPLE SNIPPET — GATE 7: BEHAVIORAL TEST
=======================================================
Stripe subscriptions + one-time payments reference implementation.
Three plans (Free/Pro/Business), Stripe Checkout hosted, webhook handler,
billing portal. Port 4014.

This file is read by RELEASE_GATE.txt Gate 7.
Every check below must PASS before this snippet can be called done.

===================================================================
SECTION A: NAVIGATION — ALL ROUTES
===================================================================

  [ ] Landing page (/) opens → shows pricing CTA
  [ ] Pricing page (/pricing) → shows 3 plans
  [ ] Login page (/auth/login) opens → shows auth methods
  [ ] Dashboard (/dashboard) redirects to login when unauthenticated
  [ ] Health check (/api/health) returns 200

===================================================================
SECTION B: SUBSCRIPTION CHECKOUT FLOW
===================================================================

  [ ] Sign in → navigate to /pricing
  [ ] Click "Upgrade to Pro" → redirects to Stripe Checkout
  [ ] Complete test payment (4242 4242 4242 4242) → returns to /dashboard
  [ ] Dashboard shows plan = "Pro", status = "active"
  [ ] subscriptions table has correct stripe_customer_id + stripe_subscription_id

===================================================================
SECTION C: BILLING PORTAL
===================================================================

  [ ] Navigate to /dashboard/billing → shows current plan + status
  [ ] Click "Manage Billing" → opens Stripe Customer Portal
  [ ] Cancel subscription in portal → returns to app
  [ ] Dashboard shows cancel_at_period_end = true

===================================================================
SECTION D: WEBHOOK HANDLING
===================================================================

  [ ] checkout.session.completed → creates/updates subscription record
  [ ] customer.subscription.updated → updates status + period_end
  [ ] customer.subscription.deleted → sets status = cancelled
  [ ] invoice.payment_failed → sets status = past_due
  [ ] Invalid signature → returns 400

===================================================================
SECTION E: SIGN OUT + ROUTE PROTECTION
===================================================================

  [ ] Sign out → clears session → redirects to /auth/login
  [ ] After sign out → /dashboard redirects to /auth/login
  [ ] Authenticated user → /auth/login redirects to /dashboard

===================================================================
WHAT CLAUDE IS NOT ALLOWED TO SUBSTITUTE FOR THIS TEST
===================================================================

  - "tsc passed" → NOT A PRODUCT TEST
  - "build succeeded" → NOT A PRODUCT TEST
  - Only the full end-to-end payment flow counts as PASS.
